const CollapsedSearchResults = function({results}) {
    return (
        <div className="CollapsedSearchResults">
            {results}
        </div>
    );
};

export default CollapsedSearchResults;